---
const appUrl = "https://app.ldr-design.com";

const navLinks = [
  { label: "Use Cases", href: "/use-cases/architectural-lighting" },
  { label: "Pricing", href: "/pricing" },
  { label: "Blog", href: "/blog" },
  { label: "App", href: appUrl, external: true },
];

const legalLinks = [
  { label: "Terms of Service", href: "/terms" },
  { label: "Privacy Policy", href: "/privacy" },
  { label: "Refund Policy", href: "/refund" },
];
---

<footer class="border-t border-border py-5 text-xs text-muted-foreground">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-3">
      <!-- Left: brand + copyright -->
      <p class="flex items-center gap-2">
        <a
          href="/"
          class="font-bold text-foreground hover:text-primary transition-colors"
        >
          LDR
        </a>
        <span aria-hidden="true" class="text-border">·</span>
        <span>© 2026 All rights reserved.</span>
      </p>

      <!-- Right: nav links + legal links, single row -->
      <nav aria-label="Footer navigation">
        <ul class="flex flex-wrap items-center gap-x-5 gap-y-2">
          {navLinks.map((link) => (
            <li>
              <a
                href={link.href}
                target={link.external ? "_blank" : undefined}
                rel={link.external ? "noopener noreferrer" : undefined}
                class="hover:text-foreground transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))}
          <li aria-hidden="true" class="text-border">·</li>
          {legalLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="hover:text-foreground transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>
    </div>
  </div>
</footer>
